{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.imagent.dev/v1/capabilities.schema.json",
  "title": "IM Agent adapter capabilities",
  "$defs": {
    "SupportLevel": {
      "enum": ["native", "fallback", "unsupported"]
    },
    "DeliverySupportLevel": {
      "enum": ["native", "fallback", "unsupported"]
    },
    "AttachmentSourceKind": {
      "enum": ["local_path", "remote_url", "attachment_handle"]
    },
    "TextLengthUnit": {
      "enum": ["code_points", "utf16_code_units", "utf8_bytes"]
    },
    "AttachmentGrouping": {
      "enum": ["none", "same_media_family", "mixed"]
    },
    "ReplyReferenceScope": {
      "enum": ["first_segment", "every_segment"]
    },
    "EventSequenceScope": {
      "enum": ["none", "thread", "application"]
    },
    "DeliveryProfile": {
      "type": "object",
      "required": [
        "plainText",
        "markdown",
        "attachments",
        "attachmentSources",
        "replyReferences"
      ],
      "properties": {
        "plainText": { "$ref": "#/$defs/DeliverySupportLevel" },
        "markdown": { "$ref": "#/$defs/DeliverySupportLevel" },
        "attachments": { "$ref": "#/$defs/DeliverySupportLevel" },
        "attachmentSources": {
          "type": "array",
          "items": { "$ref": "#/$defs/AttachmentSourceKind" },
          "uniqueItems": true
        },
        "attachmentMediaTypes": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "attachmentGrouping": { "$ref": "#/$defs/AttachmentGrouping" },
        "replyReferences": { "$ref": "#/$defs/DeliverySupportLevel" },
        "replyReferenceScope": { "$ref": "#/$defs/ReplyReferenceScope" },
        "textLengthUnit": { "$ref": "#/$defs/TextLengthUnit" },
        "maxTextLength": { "type": ["integer", "null"], "minimum": 1 },
        "maxAttachmentSize": { "type": ["integer", "null"], "minimum": 1 },
        "maxAttachmentCount": { "type": ["integer", "null"], "minimum": 1 },
        "maxAttachmentGroupSize": { "type": ["integer", "null"], "minimum": 1 }
      },
      "additionalProperties": false
    },
    "ChannelCapabilities": {
      "type": "object",
      "required": [
        "plainText",
        "markdown",
        "messageEdits",
        "attachments",
        "interactiveActions"
      ],
      "properties": {
        "plainText": { "$ref": "#/$defs/DeliverySupportLevel" },
        "markdown": { "$ref": "#/$defs/DeliverySupportLevel" },
        "messageEdits": { "$ref": "#/$defs/DeliverySupportLevel" },
        "messageDeletion": { "$ref": "#/$defs/DeliverySupportLevel" },
        "typingIndicators": { "$ref": "#/$defs/DeliverySupportLevel" },
        "interactiveActions": { "$ref": "#/$defs/DeliverySupportLevel" },
        "attachments": { "$ref": "#/$defs/DeliverySupportLevel" },
        "attachmentSources": {
          "type": "array",
          "items": { "$ref": "#/$defs/AttachmentSourceKind" },
          "uniqueItems": true
        },
        "attachmentMediaTypes": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "attachmentGrouping": { "$ref": "#/$defs/AttachmentGrouping" },
        "replyReferences": { "$ref": "#/$defs/DeliverySupportLevel" },
        "replyReferenceScope": { "$ref": "#/$defs/ReplyReferenceScope" },
        "textLengthUnit": { "$ref": "#/$defs/TextLengthUnit" },
        "nativeThreadsOrTopics": { "$ref": "#/$defs/DeliverySupportLevel" },
        "maxTextLength": { "type": ["integer", "null"], "minimum": 1 },
        "maxAttachmentSize": { "type": ["integer", "null"], "minimum": 1 },
        "maxAttachmentCount": { "type": ["integer", "null"], "minimum": 1 },
        "maxAttachmentGroupSize": { "type": ["integer", "null"], "minimum": 1 }
      },
      "additionalProperties": false
    },
    "ProjectCapabilities": {
      "type": "object",
      "required": ["mode", "discovery", "reading", "creation", "deletion"],
      "properties": {
        "mode": {
          "enum": ["managed", "flat", "fixed"]
        },
        "discovery": { "$ref": "#/$defs/SupportLevel" },
        "reading": { "$ref": "#/$defs/SupportLevel" },
        "creation": { "$ref": "#/$defs/SupportLevel" },
        "deletion": { "$ref": "#/$defs/SupportLevel" }
      },
      "allOf": [
        {
          "if": {
            "properties": { "mode": { "const": "managed" } },
            "required": ["mode"]
          },
          "then": {
            "properties": {
              "discovery": { "const": "native" },
              "reading": { "const": "native" },
              "creation": { "enum": ["native", "unsupported"] },
              "deletion": { "enum": ["native", "unsupported"] }
            }
          },
          "else": {
            "properties": {
              "discovery": { "const": "fallback" },
              "reading": { "const": "fallback" },
              "creation": { "const": "unsupported" },
              "deletion": { "const": "unsupported" }
            }
          }
        }
      ],
      "additionalProperties": false
    },
    "ThreadDeletionCapability": {
      "enum": ["unsupported", "archive", "permanent"]
    },
    "ThreadCapabilities": {
      "type": "object",
      "required": ["listing", "creation", "reading", "deletion"],
      "properties": {
        "listing": { "$ref": "#/$defs/SupportLevel" },
        "creation": { "$ref": "#/$defs/SupportLevel" },
        "reading": { "$ref": "#/$defs/SupportLevel" },
        "deletion": { "$ref": "#/$defs/ThreadDeletionCapability" }
      },
      "additionalProperties": false
    },
    "RuntimeCapabilities": {
      "type": "object",
      "required": [
        "history",
        "streaming",
        "replayFromCursor",
        "interruption",
        "interactiveRequests",
        "pendingRequestSnapshot",
        "nativeThreadActivation",
        "gapDetection",
        "eventSequenceScope"
      ],
      "properties": {
        "history": { "$ref": "#/$defs/SupportLevel" },
        "streaming": { "$ref": "#/$defs/SupportLevel" },
        "replayFromCursor": { "$ref": "#/$defs/SupportLevel" },
        "interruption": { "$ref": "#/$defs/SupportLevel" },
        "interactiveRequests": { "$ref": "#/$defs/SupportLevel" },
        "pendingRequestSnapshot": { "$ref": "#/$defs/SupportLevel" },
        "nativeThreadActivation": { "$ref": "#/$defs/SupportLevel" },
        "gapDetection": { "$ref": "#/$defs/SupportLevel" },
        "eventSequenceScope": { "$ref": "#/$defs/EventSequenceScope" }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "gapDetection": { "enum": ["native", "fallback"] }
            }
          },
          "then": {
            "properties": {
              "eventSequenceScope": { "enum": ["thread", "application"] }
            }
          }
        }
      ],
      "additionalProperties": false
    },
    "ApplicationCapabilities": {
      "type": "object",
      "required": ["projects", "threads", "runtime", "attachmentSources"],
      "properties": {
        "projects": { "$ref": "#/$defs/ProjectCapabilities" },
        "threads": { "$ref": "#/$defs/ThreadCapabilities" },
        "runtime": { "$ref": "#/$defs/RuntimeCapabilities" },
        "attachmentSources": {
          "type": "array",
          "items": { "$ref": "#/$defs/AttachmentSourceKind" },
          "uniqueItems": true
        }
      },
      "additionalProperties": false
    }
  }
}
